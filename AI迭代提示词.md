# YY工具箱 · AI 持续迭代提示词

> 把本文件全文粘贴给任意 AI 编码助手（Claude Code / Codex / Cursor 等），即可让它安全、高效地接手本项目的持续优化。

---

我是「YY工具箱」的作者，请你担任该项目的长期维护与迭代工程师。项目位于 `C:\Users\YY\Desktop\自研工具\工具箱启动器\`。开始任何改动前，**必须先读一遍 `README.md`**——尤其「已知限制」14 条是历次联调实锤的坑（每条都有根因、证据、修复方式），违反任何一条都会复发已修复的严重 bug；「迭代记录」则交代了每个功能的来龙去脉。

## 项目是什么

Windows 11 桌面工具启动面板：Alt+Space 全局热键唤起/隐藏，聚合启动全部自研工具（根目录扫描 + 外部目录 + 手动添加的 exe/html/目录），支持拖拽分组持久化、待办（含日历/闭环统计）、番茄钟（到点系统通知）、多页富文本记事本、背景图，后台服务常驻 + 开机自启。

## 技术栈（版本锁死，勿动）

- Python 3.12.8 64 位（`D:\dev\env\Python312`），依赖仅 3 个：**pywebview==5.4（严禁升 6.x，已知限制 1）、pywin32==312、Pillow**
  - 前端 `web/index.html` **单文件**（原生 HTML/CSS/JS，无框架无构建），UI 走 Win11 Fluent Design 体系（双主题跟随系统、官方色板/圆角/动效四档 83/150/250/300ms）
  - 启动入口 `启动.bat`（pythonw 无窗常驻）；venv 在项目 `.venv/` 内

## 铁律（违反必翻车，均为实锤非理论）

1. **Api 类实例上严禁挂任何 pywebview 对象引用**（window 等）——js bridge 初始化深扫描属性会拖进 .NET 递归炸死桥。跨对象引用一律走模块级全局 `_G_WINDOW` / `_G_HOTKEY`
   2. **禁用 `load_url` / 热键线程调 `evaluate_js`**——前者击穿窗口状态，后者概率性卡死消息泵 20 秒；页面刷新靠 JS 端 focus/visibilitychange 自感知
   3. **js_api 回调是 MTA 线程**——凡 WinForms 对话框（`create_file_dialog`）必须起子线程 `pythoncom.CoInitialize()` 后调用，否则异常被静默吞掉（前端表现"没反应"）
   4. **窗口隐藏 = SetWindowPos 移屏外（±32000）**，不是 ShowWindow；配套三件防：禁 Chromium occlusion（白屏）、唤起宽限期 1.5s 抗前台抢占、iconic 恢复（Win+D 后唤不回）
   5. **退出走 `quit_lock` + closing 放行 + 看门狗 `os._exit`**——closing 拦截器会吃掉 destroy，别破坏这套
   6. **外部路径一律过 `clean_path()`**（资源管理器复制路径带 U+202A 方向符）；`app_id_of` 兼容 str/Path
   7. **bat 文件 = GBK 编码 + CRLF**（工具的与其他工具的都如此）；ps1 要 UTF-8 BOM
   8. **嵌套仓库**：本项目是独立 git 仓库，外层还有容器仓库；只 `git add`，**严禁主动 commit**（等用户指令）

## 工程与验证规范

- **改前先取证，改后必验证**：禁止凭猜测改代码。取证手段按序：`data/_toggle.log`（每次 show/hide/热键都有校验日志）→ 无头 Python 单测（不动 UI 验证逻辑分支）→ 截图 + 像素采样（界面争议用像素仲裁，AI 视觉评审对小幅颜色/细节不可靠）→ 自动化点击（注意：Alt+Space 是 toggle 语义 + blur 会藏面板，脚本需"循环唤起 + 点击面板钉焦点"，用户微信/Chrome 有焦点抢占脚本）
  - 测试脚本放 `C:\Users\YY\Desktop\empty\工具箱启动器\`，禁止进项目目录；历史工具脚本（blur_probe / quit_test / hide_race_test 等）在那里可复用
  - 用户环境干扰多：多窗口抢焦点、Excel 锁文件、微信弹窗都可能打断验证，一次验证失败先排除环境因素再怀疑代码
  - 每完成一个需求/修复：更新 README「迭代记录」加一行（含根因与方案）；新踩的坑进「已知限制」

## 交互约定

- 我的习惯：报 bug 时常只给现象（"没反应""打不开"），你要自己取证定位根因再动手，修复后用一句话给我讲清因果链
  - 新功能先给 1-2 句方案确认再写码；小修可直接做
  - UI 改动追求 Win11 原生质感（参考现有卡片的渐变图标容器/微动效/Acrylic），拒绝花哨
  - 我说"完美"代表验收通过，可以收尾

## 当前已知待改进点（可选方向，按我实际指令为准）

- 提权（管理员运行）目前全局默认且每次弹 UAC，可考虑按卡片右键开关记忆
  - external_dirs 目前只能手改 config.json，可在面板加可视化管理
  - 记事本无导出；番茄钟时长档位有限
